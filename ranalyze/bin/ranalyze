#!/bin/bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && cd .. && pwd )"
source activate ranalyze-venv
python <<EOF
import runpy
import sys

sys.argv = "$@".split(" ")

sys.path.insert(1, "$DIR")

import ranalyze

runpy.run_module("ranalyze")
EOF